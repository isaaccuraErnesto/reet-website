const ReetLogo = ({ logo }) => {
  return (
    <div id='reet-logo'>
      <img
        id='nav-bar-logo'
        src={logo}
        alt='Reet Property Services Company Logo'
      />
    </div>
  );
};

export default ReetLogo;
